'use client'

import { useEffect, useState } from 'react'
import Link from 'next/link'
import AppShell from '@/components/app-shell'
import { ensureWorkspace } from '@/lib/supabase/workspace'

type Customer={id:string;company_name:string}; type Opp={id:string;title:string;status:string;stage:string;estimated_value:number|null;probability:number;customer_id:string;customers?:Customer|null;created_at:string}
const stages=['new','qualified','partner_matching','partner_selected','engagement','proposal','negotiation','won','lost']
export default function OpportunitiesPage(){
 const[items,setItems]=useState<Opp[]>([]);const[customers,setCustomers]=useState<Customer[]>([]);const[show,setShow]=useState(false);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[orgId,setOrgId]=useState('');const[form,setForm]=useState({title:'',customer_id:'',description:'',estimated_value:'',probability:'20',stage:'new',status:'open',preferred_region:'',requirements:''})
 useEffect(()=>{let active=true;(async()=>{try{const{supabase,orgId}=await ensureWorkspace();if(!active)return;setOrgId(orgId);const[o,c]=await Promise.all([supabase.from('opportunities').select('id,title,status,stage,estimated_value,probability,customer_id,customers(id,company_name),created_at').eq('org_id',orgId).order('created_at',{ascending:false}),supabase.from('customers').select('id,company_name').eq('org_id',orgId).order('company_name')]);if(o.error)throw o.error;if(c.error)throw c.error;const normalized=(o.data??[]).map((row)=>{const customer=Array.isArray(row.customers)?row.customers[0]??null:row.customers;return {...row,customers:customer}}) as Opp[];if(active){setItems(normalized);setCustomers(c.data??[]);setForm(f=>({...f,customer_id:f.customer_id||c.data?.[0]?.id||''}))}}catch(e){if(active)setError(e instanceof Error?e.message:'Could not load opportunities.')}finally{if(active)setLoading(false)}})();return()=>{active=false}},[])
 async function createOpp(e:React.FormEvent){e.preventDefault();if(!form.customer_id){setError('Add a customer before creating an opportunity.');return}setSaving(true);setError('');try{const{supabase}=await ensureWorkspace();const{data,error}=await supabase.from('opportunities').insert({org_id:orgId,customer_id:form.customer_id,title:form.title,description:form.description||null,estimated_value:form.estimated_value?Number(form.estimated_value):null,probability:Number(form.probability)||0,stage:form.stage,status:form.status,preferred_region:form.preferred_region||null,requirements:form.requirements.split(',').map(x=>x.trim()).filter(Boolean)}).select('id,title,status,stage,estimated_value,probability,customer_id,created_at').single();if(error)throw error;setItems(x=>[{...(data as Opp),customers:customers.find(c=>c.id===form.customer_id)||null},...x]);setForm({...form,title:'',description:'',estimated_value:'',requirements:''});setShow(false)}catch(e){setError(e instanceof Error?e.message:'Could not create opportunity.')}finally{setSaving(false)}}
 async function updateStage(id:string,stage:string){try{const{supabase}=await ensureWorkspace();const status=stage==='won'?'won':stage==='lost'?'lost':'open';const{error}=await supabase.from('opportunities').update({stage,status,...(status!=='open'?{probability:stage==='won'?100:0}:{})}).eq('id',id).eq('org_id',orgId);if(error)throw error;setItems(x=>x.map(o=>o.id===id?{...o,stage,status,probability:status==='won'?100:status==='lost'?0:o.probability}:o))}catch(e){setError(e instanceof Error?e.message:'Could not update opportunity.')}}
 return <AppShell title="Opportunities" subtitle="Track customer demand, route the right channel partners and keep the commercial process moving."><div className="mb-5 flex items-center justify-between gap-3"><div className="text-sm text-slate-500">{items.length} opportunities</div><button onClick={()=>setShow(v=>!v)} className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold hover:bg-blue-500">{show?'Close':'Create opportunity'}</button></div>{error&&<div className="mb-5 rounded-xl border border-red-900 bg-red-950/20 p-4 text-sm text-red-300">{error}</div>}{show&&<form onSubmit={createOpp} className="mb-5 rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3"><Input label="Opportunity title" value={form.title} req onChange={v=>setForm({...form,title:v})}/><Select label="Customer" value={form.customer_id} onChange={v=>setForm({...form,customer_id:v})} options={customers.map(c=>[c.id,c.company_name])}/><Input label="Estimated value (USD)" value={form.estimated_value} onChange={v=>setForm({...form,estimated_value:v})}/><Input label="Probability %" value={form.probability} onChange={v=>setForm({...form,probability:v})}/><Select label="Stage" value={form.stage} onChange={v=>setForm({...form,stage:v})} options={stages.map(s=>[s,s.replaceAll('_',' ')])}/><Input label="Preferred region" value={form.preferred_region} onChange={v=>setForm({...form,preferred_region:v})}/><Input label="Requirements (comma separated)" value={form.requirements} onChange={v=>setForm({...form,requirements:v})}/><label className="text-sm text-slate-300 md:col-span-2">Description<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} rows={3} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label></div><button disabled={saving} className="mt-5 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold disabled:opacity-50">{saving?'Creating…':'Create opportunity'}</button></form>}{loading?<div className="rounded-xl border border-slate-800 bg-slate-900 p-8 text-center text-slate-500">Loading pipeline…</div>:items.length===0?<div className="rounded-xl border border-dashed border-slate-800 p-10 text-center"><p className="font-medium">No opportunities yet.</p><p className="mt-2 text-sm text-slate-500">Create a customer first, then create your first channel opportunity.</p><Link href="/customers" className="mt-4 inline-block text-sm text-blue-400">Go to customers →</Link></div>:<div className="space-y-3">{items.map(o=><article key={o.id} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{o.title}</h2><span className="rounded-full border border-slate-700 px-2.5 py-1 text-[11px] text-slate-400">{o.status}</span></div><p className="mt-1 text-sm text-slate-500">{o.customers?.company_name||customers.find(c=>c.id===o.customer_id)?.company_name||'Customer'} · {o.estimated_value?`$${Number(o.estimated_value).toLocaleString()}`:'Value not set'} · {o.probability}% probability</p></div><select value={o.stage} onChange={e=>void updateStage(o.id,e.target.value)} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-2 text-sm capitalize text-slate-200"><option value="new">New</option>{stages.slice(1).map(s=><option key={s} value={s}>{s.replaceAll('_',' ')}</option>)}</select></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-blue-500 transition-all" style={{width:`${Math.max(0,Math.min(100,o.probability))}%`}}/></div></article>)}</div>}</AppShell>}
function Input({label,value,onChange,req}:{label:string;value:string;onChange:(v:string)=>void;req?:boolean}){return <label className="text-sm text-slate-300">{label}<input required={req} value={value} onChange={e=>onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label>}
function Select({label,value,onChange,options}:{label:string;value:string;onChange:(v:string)=>void;options:string[][]}){return <label className="text-sm text-slate-300">{label}<select value={value} onChange={e=>onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm outline-none focus:border-blue-500"><option value="">Select…</option>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
